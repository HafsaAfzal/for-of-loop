// Create an array called `fruits` with some fruit names. Use a `for...of` loop to iterate
// over the array and log each fruit name.
let fruits=["apple","mango","banana","apricot"];
for (val of fruits){
    console.log(val);
}