// Create a string called `message` with some text. Use a `for...of` loop to iterate over
// the characters of the string and log each character.
let message="Hello! Hafsa";
for ( str of message){
    console.log(str);
}